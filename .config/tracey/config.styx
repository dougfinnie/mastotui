specs (
    {
        name mastotui
        include (docs/spec/**/*.md)
        impls (
            {
                name rust
                include (src/**/*.rs)
                exclude (target/**)
                test_include (tests/**/*.rs)
            }
        )
    }
)
